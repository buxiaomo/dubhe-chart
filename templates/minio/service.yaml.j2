apiVersion: v1
kind: Service
metadata:
  name: minio
spec:
  externalTrafficPolicy: Cluster
  type: NodePort
  selector:
    app: minio
  ports:
    - name: tcp
      port: 9000
      targetPort: 9000
      nodePort: 30900
