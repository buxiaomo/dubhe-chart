---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-vol
  labels:
    app: minio
spec:
  storageClassName: "{{ .Values.global.storageClassName }}"
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: 1Gi
